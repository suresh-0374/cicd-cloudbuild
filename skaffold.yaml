apiVersion: skaffold/v3
kind: Config
metadata:
  name: my-app
manifests:
  rawYaml:
    - cloudrun.yaml # This MUST match the actual filename in your repo
deploy:
  cloudrun: {}    # Changed from cloudRun to cloudrun (lowercase 'r')
